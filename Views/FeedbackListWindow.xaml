<Window x:Class="AssignmentPRN212.Views.FeedbackListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quản lý đánh giá" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Quản lý đánh giá" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Search and Actions -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="SearchTextBox" Width="300" Height="35" Padding="10" 
                     FontSize="14" VerticalContentAlignment="Center"
                     TextChanged="SearchTextBox_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border Background="White" CornerRadius="5" BorderThickness="1" BorderBrush="#E0E0E0" Padding="10">
                                        <ScrollViewer x:Name="PART_ContentHost"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TextBox.Style>
            </TextBox>
            <TextBlock Text="🔍 Tìm kiếm..." FontSize="12" Foreground="#999" 
                       VerticalAlignment="Center" Margin="10,0,0,0" IsHitTestVisible="False"/>
            <Button Content="🔄 Làm mới" Width="120" Height="35" Margin="20,0,0,0"
                    Background="#2196F3" Foreground="White" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand" Click="RefreshButton_Click"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="FeedbackDataGrid" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="FeedbackDataGrid_SelectionChanged"
                  Background="White" BorderThickness="0" GridLinesVisibility="None"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Tiêu đề" Binding="{Binding Title}" Width="200"/>
                <DataGridTextColumn Header="Nội dung" Binding="{Binding Content}" Width="300">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="MaxHeight" Value="60"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="User ID" Binding="{Binding UserId}" Width="80"/>
                <DataGridTextColumn Header="Order ID" Binding="{Binding RentalOrderId}" Width="80"/>
                <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAt, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                <DataGridTemplateColumn Header="Thao tác" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Sửa" Width="60" Height="30" Margin="0,0,5,0"
                                        Background="#2196F3" Foreground="White" FontSize="12"
                                        BorderThickness="0" Cursor="Hand"
                                        Click="EditButton_Click" Tag="{Binding}"/>
                                <Button Content="Xóa" Width="60" Height="30"
                                        Background="#F44336" Foreground="White" FontSize="12"
                                        BorderThickness="0" Cursor="Hand"
                                        Click="DeleteButton_Click" Tag="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0">
            <TextBlock Text="Tổng số: " FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock x:Name="TotalCountTextBlock" Text="0" FontSize="14" FontWeight="Bold" 
                       Foreground="#4CAF50" VerticalAlignment="Center" Margin="5,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>

