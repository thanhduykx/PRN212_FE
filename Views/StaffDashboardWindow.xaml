<Window x:Class="AssignmentPRN212.Views.StaffDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trang chủ - Nhân viên" Height="800" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Xin chào, Nhân viên!" FontSize="24" FontWeight="Bold" Foreground="#FF9800" Margin="0,0,0,10"/>
                <TextBlock x:Name="WelcomeTextBlock" Text="Quản lý giao nhận xe và khách hàng" FontSize="14" Foreground="#666"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Column -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Quản lý giao - nhận xe" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <Button Content="📦 Giao xe" Width="300" Height="60" Margin="0,0,0,10"
                        Background="#4CAF50" Foreground="White" FontSize="16" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="CheckInButton_Click">

                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#45A049"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="📥 Nhận xe" Width="300" Height="60" Margin="0,0,0,10"
                        Background="#2196F3" Foreground="White" FontSize="16" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="CheckOutButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1976D2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="🚗 Xe đang cho thuê" Width="300" Height="60" Margin="0,0,0,10"
                        Background="#FF9800" Foreground="White" FontSize="16" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="ActiveRentalsButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F57C00"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="📍 Xe tại địa điểm" Width="300" Height="60" Margin="0,0,0,10"
                        Background="#9C27B0" Foreground="White" FontSize="16" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="PaymentButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#7B1FA2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="📋 Quản lý đơn hàng" Width="300" Height="60" Margin="0,0,0,10"
                        Background="#00BCD4" Foreground="White" FontSize="16" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="VerifyCustomerButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#0097A7"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="🚗 Quản lý xe" Width="300" Height="60" Margin="0,0,0,10"
                        Background="#E91E63" Foreground="White" FontSize="16" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="ManageCarsButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#C2185B"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <!-- Right Column - Quick Info -->
            <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="20">
                <StackPanel>
                    <TextBlock Text="Thông tin nhanh" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <TextBlock Text="📊 Thống kê hôm nay" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="TodayDeliveriesTextBlock" Text="Số lượt giao: 0" FontSize="14" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TodayReturnsTextBlock" Text="Số lượt nhận: 0" FontSize="14" Margin="0,0,0,5"/>
                    <TextBlock x:Name="AvailableCarsTextBlock" Text="Xe có sẵn: 0" FontSize="14" Margin="0,0,0,20"/>

                    <TextBlock Text="💰 Doanh thu" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#4CAF50"/>
                    <TextBlock x:Name="RevenueTextBlock" Text="Tổng doanh thu: 0 VNĐ" FontSize="14" Margin="0,0,0,5" Foreground="#2E7D32" FontWeight="SemiBold"/>
                    <TextBlock x:Name="ThisMonthRevenueTextBlock" Text="Doanh thu tháng này: 0 VNĐ" FontSize="14" Margin="0,0,0,20" Foreground="#388E3C"/>

                    <TextBlock Text="📋 Đơn chờ xử lý" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <ListBox x:Name="PendingOrdersListBox" Height="200" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F5F5F5" CornerRadius="5" Padding="10" Margin="0,0,0,5">
                                    <StackPanel>
                                        <TextBlock Text="{Binding OrderDate, StringFormat='Đơn #{0}'}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Status}" FontSize="12" Foreground="#666"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>

