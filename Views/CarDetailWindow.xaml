<Window x:Class="AssignmentPRN212.Views.CarDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chi tiết xe" Height="800" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Car Info -->
        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                    <Image x:Name="CarImage" Height="200" Stretch="UniformToFill" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Image x:Name="CarImage2" Width="80" Height="60" Stretch="UniformToFill" Margin="0,0,5,0" 
                               MouseLeftButtonDown="Image_MouseLeftButtonDown" Cursor="Hand" Visibility="Collapsed"/>
                        <Image x:Name="CarImage3" Width="80" Height="60" Stretch="UniformToFill" 
                               MouseLeftButtonDown="Image_MouseLeftButtonDown" Cursor="Hand" Visibility="Collapsed"/>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="CarNameTextBlock" Text="" FontSize="28" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="CarModelTextBlock" Text="" FontSize="18" Foreground="#666" Margin="0,0,0,15"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="👥 Số chỗ:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock x:Name="CarSeatsTextBlock" Text="" FontSize="16" Margin="0,0,0,10"/>
                            <TextBlock Text="🔋 Pin:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock x:Name="CarBatteryTextBlock" Text="" FontSize="16" Margin="0,0,0,10"/>
                            <TextBlock Text="📦 Cốp:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock x:Name="CarTrunkTextBlock" Text="" FontSize="16"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="💰 Giá thuê/ngày:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock x:Name="CarPricePerDayTextBlock" Text="" FontSize="18" FontWeight="Bold" Foreground="#4CAF50" Margin="0,0,0,10"/>
                            <TextBlock Text="💰 Giá thuê/giờ:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock x:Name="CarPricePerHourTextBlock" Text="" FontSize="16" Margin="0,0,0,10"/>
                            <TextBlock Text="🚗 Loại:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock x:Name="CarSizeTypeTextBlock" Text="" FontSize="16"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Feedback Section -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="💬 Đánh giá" FontSize="20" FontWeight="Bold"/>
                    <TextBlock x:Name="FeedbackCountTextBlock" Text="(0 đánh giá)" FontSize="14" Foreground="#666" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
                    <ItemsControl x:Name="FeedbacksItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F9F9F9" CornerRadius="5" Padding="15" Margin="0,0,0,10" BorderThickness="1" BorderBrush="#E0E0E0">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                            <TextBlock Text="👤" FontSize="18" Margin="0,0,10,0"/>
                                            <TextBlock x:Name="UserNameTextBlock" Text="Người dùng" FontWeight="SemiBold" FontSize="14"/>
                                            <TextBlock Text="•" Margin="10,0,10,0" Foreground="#999"/>
                                            <TextBlock x:Name="FeedbackDateTextBlock" Text="" FontSize="12" Foreground="#666"/>
                                        </StackPanel>
                                        <TextBlock x:Name="FeedbackTitleTextBlock" Text="{Binding Title}" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
                                        <TextBlock x:Name="FeedbackContentTextBlock" Text="{Binding Content}" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                        <StackPanel x:Name="ActionButtonsPanel" Orientation="Horizontal" HorizontalAlignment="Right" Visibility="Collapsed">
                                            <Button Content="Sửa" Width="60" Height="30" Margin="0,0,5,0"
                                                    Background="#2196F3" Foreground="White" FontSize="12"
                                                    BorderThickness="0" Cursor="Hand"
                                                    Click="EditFeedbackButton_Click" Tag="{Binding}"/>
                                            <Button Content="Xóa" Width="60" Height="30"
                                                    Background="#F44336" Foreground="White" FontSize="12"
                                                    BorderThickness="0" Cursor="Hand"
                                                    Click="DeleteFeedbackButton_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Button Grid.Row="2" Content="➕ Thêm đánh giá" Width="150" Height="40"
                        Background="#4CAF50" Foreground="White" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="AddFeedbackButton_Click"
                        HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Đóng" Width="120" Height="40" Margin="0,0,10,0"
                    Background="#9E9E9E" Foreground="White" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand" Click="CloseButton_Click"/>
            <Button Content="Thuê xe" Width="150" Height="40"
                    Background="#4CAF50" Foreground="White" FontSize="16" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand" Click="RentCarButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

