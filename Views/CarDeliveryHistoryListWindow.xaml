<Window x:Class="AssignmentPRN212.Views.CarDeliveryHistoryListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Danh sách Lịch sử giao xe" Height="600" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Box -->
        <Border Grid.Row="0" Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔍" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="SearchTextBox" Width="300" Height="30" 
                         Padding="5" VerticalContentAlignment="Center"
                         TextChanged="SearchTextBox_TextChanged"
                         Tag="Tìm kiếm theo OrderId, CarId, Odometer..."/>
                <TextBlock Text="Tổng số: " FontWeight="SemiBold" VerticalAlignment="Center" Margin="20,0,5,0"/>
                <TextBlock x:Name="TotalCountTextBlock" Text="0" FontWeight="Bold" VerticalAlignment="Center" Foreground="#2196F3"/>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid x:Name="CarDeliveryHistoryDataGrid" AutoGenerateColumns="False" SelectionMode="Single"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  Grid.Row="1" Margin="0,0,0,10" 
                  HeadersVisibility="Column"
                  RowBackground="#FFFFFF" AlternatingRowBackground="#F7F7F7"
                  GridLinesVisibility="None"
                  SelectionChanged="CarDeliveryHistoryDataGrid_SelectionChanged"
                  FontSize="12" FontWeight="Normal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Tên xe" Binding="{Binding CarName}" Width="150"/>
                <DataGridTextColumn Header="Số điện thoại" Binding="{Binding PhoneNumber}" Width="120"/>
                <DataGridTextColumn Header="Ngày đặt" Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Thời gian nhận" Binding="{Binding PickupTime, StringFormat=dd/MM/yyyy HH:mm}" Width="130"/>
                <DataGridTextColumn Header="Thời gian trả dự kiến" Binding="{Binding ExpectedReturnTime, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                <DataGridTextColumn Header="Có tài xế" Binding="{Binding WithDriverText}" Width="80"/>
                <DataGridTextColumn Header="Phí tài xế" Binding="{Binding DriverFeeText}" Width="120"/>
                <DataGridTextColumn Header="Đặt cọc" Binding="{Binding Deposit, StringFormat={}{0:N0} VNĐ, TargetNullValue=0 VNĐ}" Width="120"/>
                <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalText}" Width="120"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Panel -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="15" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Chọn đơn hàng và nhấn 'Giao xe' để chuyển trạng thái sang 'Đang cho thuê'" 
                          FontSize="14" VerticalAlignment="Center" Margin="0,0,20,0" Foreground="#666"/>
                <Button x:Name="DeliverCarButton" Content="🚗 Giao xe" Width="150" Height="40" 
                        Background="#4CAF50" Foreground="White" FontSize="16" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="DeliverCarButton_Click"
                        IsEnabled="False">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#45A049"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#CCCCCC"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="🔄 Làm mới" Width="120" Height="40" Margin="10,0,0,0"
                        Background="#FF9800" Foreground="White" FontSize="14" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="RefreshButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F57C00"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>

