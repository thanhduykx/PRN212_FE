<Window x:Class="AssignmentPRN212.Views.HomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AssignmentPRN212.Views"
        Title="Thuê xe điện - Trang chủ" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Window.Resources>
        <local:ImageUrlConverter x:Key="ImageUrlConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="🚗 Hệ thống Thuê xe Điện" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Thuê xe điện tiện lợi, nhanh chóng" FontSize="14" Foreground="#E3F2FD" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock x:Name="UserInfoTextBlock" Text="Chưa đăng nhập" FontSize="14" Foreground="White" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button x:Name="LoginButton" Content="Đăng nhập" Width="100" Height="35" 
                            Background="White" Foreground="#2196F3" FontWeight="SemiBold"
                            BorderThickness="0" Cursor="Hand" Click="LoginButton_Click"/>
                    <Button x:Name="DashboardButton" Content="Dashboard" Width="100" Height="35" Margin="10,0,0,0"
                            Background="#4CAF50" Foreground="White" FontWeight="SemiBold"
                            BorderThickness="0" Cursor="Hand" Click="DashboardButton_Click" Visibility="Collapsed"/>
                    <Button x:Name="LogoutButton" Content="Đăng xuất" Width="100" Height="35" Margin="10,0,0,0"
                            Background="#F44336" Foreground="White" FontWeight="SemiBold"
                            BorderThickness="0" Cursor="Hand" Click="LogoutButton_Click" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search and Filter -->
            <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Tìm kiếm xe" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox x:Name="SearchTextBox" Grid.Column="0" Height="40" Padding="10" 
                                 VerticalContentAlignment="Center" FontSize="14"
                                 TextChanged="SearchTextBox_TextChanged"
                                 Tag="Tìm kiếm theo tên, model, loại xe..."/>
                        
                        <ComboBox x:Name="SizeTypeComboBox" Grid.Column="1" Width="150" Height="40" Margin="10,0"
                                  SelectionChanged="FilterComboBox_SelectionChanged">
                            <ComboBoxItem Content="Tất cả kích thước" IsSelected="True"/>
                            <ComboBoxItem Content="Nhỏ"/>
                            <ComboBoxItem Content="Trung bình"/>
                            <ComboBoxItem Content="Lớn"/>
                        </ComboBox>
                        
                        <Button Content="🔍 Tìm kiếm" Grid.Column="2" Width="120" Height="40" Margin="10,0,0,0"
                                Background="#2196F3" Foreground="White" FontWeight="SemiBold"
                                BorderThickness="0" Cursor="Hand" Click="SearchButton_Click"/>
                    </Grid>
                    
                    <TextBlock x:Name="TotalCarsTextBlock" Text="Tìm thấy 0 xe" FontSize="14" Foreground="#666" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Car List -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="CarsItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3" HorizontalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="10" Padding="15" Margin="5" 
                                    BorderThickness="1" BorderBrush="#E0E0E0"
                                    MouseEnter="CarCard_MouseEnter" MouseLeave="CarCard_MouseLeave"
                                    Cursor="Hand" MouseLeftButtonDown="CarCard_MouseLeftButtonDown">
                                <StackPanel>
                                    <Image Source="{Binding ImageUrl, Converter={StaticResource ImageUrlConverter}}" Height="220" Stretch="Uniform" 
                                           Margin="0,0,0,10" HorizontalAlignment="Stretch"/>
                                    <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Model}" FontSize="14" Foreground="#666" Margin="0,0,0,5"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="👥 " FontSize="14"/>
                                        <TextBlock Text="{Binding Seats}" FontSize="14"/>
                                        <TextBlock Text=" chỗ" FontSize="14" Margin="5,0,0,0"/>
                                        <TextBlock Text="🔋 " FontSize="14" Margin="15,0,0,0"/>
                                        <TextBlock Text="{Binding BatteryDuration}" FontSize="14"/>
                                        <TextBlock Text=" km" FontSize="14" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding SizeType}" FontSize="12" Foreground="#2196F3" Margin="0,0,0,10"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="💰 " FontSize="16"/>
                                        <TextBlock Text="{Binding RentPricePerDay, StringFormat={}{0:N0}}" 
           FontSize="18" FontWeight="Bold" Foreground="#4CAF50"/>

                                        <TextBlock Text=" VNĐ/ngày" FontSize="12" Foreground="#666" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding StatusText}" FontSize="12" 
                                               FontWeight="SemiBold" HorizontalAlignment="Right">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="0">
                                                        <Setter Property="Foreground" Value="Green"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="1">
                                                        <Setter Property="Foreground" Value="Red"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    
                                    <!-- Feedback Preview -->
                                    <Border Background="#F0F0F0" CornerRadius="5" Padding="8" Margin="0,10,0,5">
                                        <StackPanel>
                                            <TextBlock x:Name="FeedbackPreviewTextBlock" Text="💬 Xem đánh giá" 
                                                       FontSize="12" Foreground="#666" Cursor="Hand"
                                                       MouseLeftButtonDown="FeedbackPreview_MouseLeftButtonDown"
                                                       Tag="{Binding}"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                                        <Button Content="Chi tiết" Width="80" Height="30" Margin="0,0,5,0"
                                                Background="#2196F3" Foreground="White" FontSize="12"
                                                BorderThickness="0" Cursor="Hand"
                                                Click="ViewDetailButton_Click" Tag="{Binding}"/>
                                        <Button Content="Thuê ngay" Width="80" Height="30"
                                                Background="#4CAF50" Foreground="White" FontSize="12" FontWeight="SemiBold"
                                                BorderThickness="0" Cursor="Hand"
                                                Click="RentCarButton_Click"
                                                Tag="{Binding}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>

